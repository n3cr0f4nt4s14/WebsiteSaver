{
  "name": "websitesaver",
  "version": "0.1.0",
  "description": "A browser add-on for saving websites.",
  "author": "Necrofantasia",
  "license": "MIT",
  "scripts": {
    "start": "echo \"Error: no start specified\" && exit 1",
    "build": "echo \"Error: no build specified\" && exit 1",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "ts-loader": "^9.4.2",
    "webpack": "^5.82.1",
    "webpack-cli": "^5.1.1"
  }
}
